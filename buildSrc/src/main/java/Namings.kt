/**
 *   @author xavierjimenez
 *   @since 8/3/23
 *   @email xavijimenezmulet@macaqueconsulting.com
 */
object Namings {
    private const val rootPackage = "com.xavijimenezmulet."
    const val appModule = "${rootPackage}mobilechallenge"
    const val componentModule = "${rootPackage}component"
    const val constantsModule = "${rootPackage}constants"
    const val frameworkModule = "${rootPackage}framework"
    const val providerModule = "${rootPackage}provider"
    const val testutilsModule = "${rootPackage}testutils"
    const val themeModule = "${rootPackage}theme"
    const val utilsModule = "${rootPackage}utils"
    const val entityModule = "${rootPackage}entity"
    const val localModule = "${rootPackage}local"
    const val modelModule = "${rootPackage}model"
    const val remoteModule = "${rootPackage}remote"
    const val repositoryModule = "${rootPackage}repository"
    const val usecaseModule = "${rootPackage}usecase"
    const val splashModule = "${rootPackage}splash"
    const val welcomeModule = "${rootPackage}welcome"
    const val homeModule = "${rootPackage}home"
    const val productsModule = "${rootPackage}products"
    const val settingsModule = "${rootPackage}settings"
}